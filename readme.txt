The SQL specifics will need to be set up for your local SQL settings. Also your own discord auth code will need to be provided as the argument in the bot.run()

To run the bot you can simply double click the batch file. It only contains:
"python3 botmain.py"
pause

the pause is to be able to help check for potential issues